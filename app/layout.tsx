import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LifeMap - Find Your Perfect Home, Tailored to Your Lifestyle',
  description: 'Personalized property search based on your commute, hobbies, green spaces, and air quality. AI-driven recommendations for the modern homebuyer.',
  keywords: 'property search, lifestyle, real estate, PropTech, AI, home search, personalized',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
} 